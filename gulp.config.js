/** 
 * @param {"dev"|"prod"|"development"|"production"} mode - project build mode
 * @param {"js"|"ts"} scriptType - type of script that you will use in project
 */
const config = {
  mode: "prod",
  scriptType: "js",
}

exports = module.exports = config